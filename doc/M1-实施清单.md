# hi-im-api M1 实施清单

> **对应生态里程碑**：档 C **M2**（与 hi-im-hubclient 并行）  
> **工期**：约 1.5～2 周（全职）  
> **前置**：beehive-im 源码只读对照

---

## Week 1 · 骨架 + header + cmd

### 1.1 工程初始化

- [x] `go mod init github.com/<org>/hi-im-api`
- [x] `LICENSE`（Apache 2.0）、`README.md`
- [x] `.gitignore`、`Makefile`（test / proto-gen）
- [x] GitHub Actions：`go test ./...`

### 1.2 `pkg/im/header`

- [x] `header.go` — `Header` struct，`Size = 52`（beehive wire 布局；文档「48B」为历史简称）
- [x] `encode.go` / `decode.go` — 大端，字段偏移与 beehive 一致
- [x] `validate.go` — `Validate(flags)`（nid/sid 规则）
- [x] `test/header_test.go` — 与 beehive `MesgHeadHton` 字节 golden 对比

### 1.3 `pkg/im/cmd`

- [x] 迁移 **M2 最小集** CMD：
  - [x] 0x0101 ONLINE / 0x0105 PING
  - [x] 0x0305 GROUP_JOIN / 0x030B GROUP_CHAT
  - [x] 0x0405 ROOM_JOIN / 0x040B ROOM_CHAT（可选 M2 末）
- [x] 注释标明 ACK 与 NTF 成对关系

### 1.4 `pkg/im/const`

- [x] `LSND_TYPE_WS`、`CHAT_*_TTL` 等从 `comm/const.go` 迁移

**Week 1 验收**：`go test ./pkg/im/...` 全绿。

---

## Week 2 · proto + rediskey + seq + 集成

### 2.1 `proto/im/v1/mesg.proto`

- [x] proto3，`package hi.im.v1`
- [x] M2 消息：`Online`、`OnlineAck`、`GroupJoin`、`GroupChat`（+ ACK）
- [x] `make proto` → `gen/go/im/v1/*.pb.go`

### 2.2 `proto/seq/v1/seq.proto`

- [x] `SeqService`：AllocSid/Rid/Gid/Seq
- [x] `make proto-grpc` → `gen/go/seq/v1/*_grpc.pb.go`

### 2.3 `pkg/rediskey`

- [x] 迁移 fan-out 相关键：
  - [x] `im:sid:%d:attr`
  - [x] `chat:gid:%d:to:nid:zset`
  - [x] `chat:gid:%d:to:uid:zset`
  - [x] `room:rid:%d:to:nid:zset`（chatroom，与 beehive 一致）
- [x] `test/rediskey_test.go`

### 2.4 `pkg/errno` + `pkg/httpx`

- [x] 迁移 `ERR_SYS_*`、`ERR_SVR_*`
- [x] `httpx.Response` JSON 壳

### 2.5 文档与发版

- [x] 核对 [技术设计文档.md](技术设计文档.md)
- [ ] 打 tag **`v0.1.0`**
- [ ] 通知 hi-im-hubclient 仓库 `go get` 联调

**M2 总验收**：

| 项 | 标准 |
|----|------|
| header | 48B golden 测试通过 |
| cmd/proto | 群聊/上线最小集可用 |
| seq gRPC | 生成代码可编译 |
| rediskey | gid→nid 键与 beehive 一致 |
| semver | v0.1.0 tag |

---

## 依赖关系

| 下游 | 需要本仓库 |
|------|------------|
| **hi-im-hubclient** | `pkg/im/cmd`、`pkg/im/header`（组包/测包） |
| **hi-im-usrsvr** | header、proto、errno、httpx、seq client |
| **hi-im-msgsvr** | header、proto、rediskey、cmd |
| **hi-im-gateway** | header、proto、cmd |

---

## 建议提交节奏

```text
commit 1  chore: go mod, ci, header types
commit 2  feat(header): encode/decode + golden tests
commit 3  feat(cmd,const): M2 minimal commands
commit 4  feat(rediskey,errno,httpx)
commit 5  feat(proto): im/v1 + seq/v1 + gen
commit 6  docs: align technical design; tag v0.1.0
```

---

*清单版本：2026-07-02*
